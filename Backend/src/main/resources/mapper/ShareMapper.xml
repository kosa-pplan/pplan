<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.pplan.repository.mapper.ShareMapper">

    <!-- 좋아요 여부 확인 -->
    <select id="checkLike" parameterType="org.pplan.service.dto.share.ShareDTO" resultType="int">
        SELECT ID
        FROM T_Share
        WHERE T_REVIEW_ID = #{reviewId} AND T_USER_ID = #{userId}
    </select>

    <!-- boolean 좋아요 여부 확인 -->
    <select id="checkedLikeCount" parameterType="org.pplan.service.dto.share.ShareDTO" resultType="int">
        SELECT COUNT(*)
        FROM T_Share
        WHERE T_REVIEW_ID = #{reviewId} AND T_USER_ID = #{userId}
    </select>

    <!--좋아요 추가-->
    <insert id="addLike" parameterType="org.pplan.service.dto.share.ShareDTO">
        INSERT INTO T_Share (T_REVIEW_ID, T_USER_EMAIL, T_USER_ID)
        VALUES (#{reviewId}, #{userEmail}, #{userId})
    </insert>

    <!--좋아요 삭제-->
    <delete id="removeLike" parameterType="org.pplan.service.dto.share.ShareDTO">
        DELETE FROM T_Share
        WHERE T_REVIEW_ID = #{reviewId} AND T_USER_ID = #{userId}
    </delete>

    <!--좋아요 갯수 조회-->
    <select id="getShareCount" parameterType="org.pplan.service.dto.share.ShareDTO" resultType="long">
        SELECT COUNT(*) AS likeCount
        FROM T_Share
        WHERE T_REVIEW_ID = #{reviewId}
    </select>

</mapper>